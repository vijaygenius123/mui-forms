import {AppBar, Toolbar, Typography} from "@mui/material";

export const Header = () => {
    return (
        <AppBar>
            <Toolbar>
                <Typography variant={"h5"}>App</Typography>
            </Toolbar>
        </AppBar>
    )
}
